1、如何将如下的JSON正确解析成 Object
{
  "userId": 111323290434354540545
}

  回答：
  var stringObj = {
    "userId": 111323290434354540545
  }
  var obj = JSON.parse(stringObj)

2、前端需要稳定每隔1s向服务端请求API, 请问如何实现？
  回答：可以使用JavaScript的`setInterval`方法

3、什么情况下，你会为你的项目引入状态管理库，比如Redux, Pinia, 可以简述一下起到了什么作用么？
  回答：需要大量使用到全局变量时，起到存储数据的作用

4、为什么ESM与CJS不能兼容？
  回答： ESM 和 CJS 完全是两套不同的设计。表面上，ESM 使用起来虽然有点接近 CJS，但是实现差异巨大。